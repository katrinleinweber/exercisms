parse_phone_number <- function(number_string) {
  
  # reject, if not starting with 1 or not 
  
  # find regex to remove any punctuation
  
  # reject if not exactly 10 digits long or 1 as 4th digit
  
}
